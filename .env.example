# PostgreSQL (used by the db service)
POSTGRES_USER=myuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=taskflow

# Application (used by the api service)
DATABASE_URL=postgresql+asyncpg://myuser:changeme@db:5432/taskflow
